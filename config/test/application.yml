spring:
  datasource:
    hikari:
      connection-test-query: SELECT 1 FROM DUAL
      minimum-idle: 1
      maximum-pool-size: 5
    driver-class-name: com.mysql.jdbc.Driver
    jdbc-url:  jdbc:mysql://prodex-replicatest.cedlveyttji9.us-east-2.rds.amazonaws.com:3306/birzha?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: devprod
    password: Fh24soaf8sy3
    initialization-mode: always
  jpa:
    hibernate:
      ddl-auto: none
# --- server
server:
  port: 8080

redis:
  server:
    port: 6379
    host: localhost